<?xml version="1.0" encoding="utf-8"?>
<!--
  Transport security.

  HTTPS everywhere by default, which is what section 45 requires. The single
  exception is cleartext to the two addresses that mean "the machine running
  `npm run dev`" — a developer's own API on a LAN or an emulator host, which has
  no certificate and never will.

  Anything else, including any production host, is refused in cleartext by the
  platform before a request is made. That is deliberately not a runtime check
  the app could be talked out of.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <domain-config cleartextTrafficPermitted="true">
        <!-- The emulator's alias for the host machine. -->
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
