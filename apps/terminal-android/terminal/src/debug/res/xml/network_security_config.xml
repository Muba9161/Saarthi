<?xml version="1.0" encoding="utf-8"?>
<!--
  Transport security, debug builds only.

  This file replaces `main`'s copy in a debug build and does one thing to it:
  adds the loopback address, so a phone tethered over USB with
  `adb reverse tcp:4000 tcp:4000` can reach the developer's own API. `localhost`
  already worked; `127.0.0.1` is the same machine written the way people
  actually type it, and a build that refuses one but not the other is a
  half-hour nobody gets back.

  The release build is untouched and still refuses cleartext to anything but the
  two dev aliases — the point of putting this in `src/debug` rather than
  loosening the shared config is that a developer convenience cannot follow the
  app onto a vehicle.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <domain-config cleartextTrafficPermitted="true">
        <!-- The emulator's alias for the host machine. -->
        <domain includeSubdomains="false">10.0.2.2</domain>
        <!-- A USB-tethered phone, through `adb reverse`. -->
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
